

What happens when you initialize a repository? Why do you need to do it?

Answer: It creates the hidden .git file that you can use to commit changes to.. yay

How is the staging area different from the working directory and the repository?

Answer: It is not something you edit directly. Yet it is also not something stored in .git directoy

What value do you think it offers?

It allows you to bundle lots of things together. Commit them all at once, instead of in atomistic commits.

How can you use the staging area to make sure you have one commit per logical
change?

Only make the commit once you have made all those changes.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?
Answer: I have a lot of caffiene in my body and I'm ansy and I want to get out of this office and enjoy my friday now.

Real Answer: You can make a branch called "special feature" o r "some other thing" or "colorized etc". You can than work on these feature and opossible merge them later.

I have not yet been through what merge is.


How do the diagrams help you visualize the branch structure?
Answer: Each Commit remembers only a single, one way, link on a chain to its parent. Branches help visualize this relationship.
What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

What are the pros and cons of Git’s automatic merging vs. always doing merges
manually?

Answer: Automatic Merging is quick and easy. It is best done when one branch's code do not overlap with another branch's code. That is only one of the two branches changes the common ancestor. While the other only adds new code.

Manual merging is probably the best and important when more than one person is working on a code. Or when a new branch is created to perhaps clean up previous code. E.g. Turning long sections of code into fuctions/processes, refractoring, etc.